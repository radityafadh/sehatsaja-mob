import 'package:flutter/material.dart';

class ExperienceContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('This is the experience section content.');
  }
}
